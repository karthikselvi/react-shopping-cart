import React, { useState } from 'react'
import './cart.css'

const Cart=({cart,setcart})=> {
    const [price,setprice]=useState(0);
  return (
    <article>

    </article>
  )
}

export default Cart